#comandos 

#ativar ambiente virtual
venv/bin/activate

#atualizar pip
python -m pip install --upgrade pip

#valid SSH key
ls -al ~/.ssh
ssh-keygen -t ed25519 -C edu.mneto@hotmail.com
sudo cat ~/.ssh/id_ed25519
sudo cat ~/.ssh/id_ed25519.pub
eval "$(ssh-agent -s)"
ssh-add ~/.ssh/id_ed25519
ssh -T git@github.com



#Reescrever o histórico (se já comitou coisas gigantes)
git filter-repo --path arquivo_grande --invert-paths


#apagar commits
git fetch origin
git reset --hard origin/main